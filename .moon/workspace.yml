$schema: 'https://moonrepo.dev/schemas/workspace.json'

projects:
  contract: 'packages/contract'
  web: 'apps/web'
  inference: 'apps/inference'
  mobile: 'apps/mobile'

vcs:
  client: 'git'
  defaultBranch: 'main'
